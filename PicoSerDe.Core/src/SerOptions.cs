namespace PicoSerDe.Core;

/// <summary>
/// Base class for all format options (JsonOptions, IniOptions, ...).
/// Carried explicitly through serialize/deserialize calls — no ambient state.
/// </summary>
public class SerOptions { }
